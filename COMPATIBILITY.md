# Compatibility

New portable publication backups are written as `.colophon` files with the `colophon-portable` format identifier.

For migration, Colophon can also import legacy `.sabotpress` files whose internal format identifier is `sabotpress-portable`. Legacy names are accepted only at the import boundary; new exports, application identifiers, cache keys, events, documentation, and package metadata use Colophon naming.
