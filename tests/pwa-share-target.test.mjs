import assert from 'node:assert/strict';import fs from 'node:fs';import test from 'node:test'
test('manifest declares multipart Web Share Target with media',()=>{const m=JSON.parse(fs.readFileSync(new URL('../public/site.webmanifest',import.meta.url)));assert.equal(m.share_target.action,'./share-target');assert.equal(m.share_target.method,'POST');assert.ok(m.share_target.params.files[0].accept.includes('application/pdf'))})
test('service worker captures local share POST into IndexedDB and redirects to review',()=>{const s=fs.readFileSync(new URL('../public/sw.js',import.meta.url),'utf8');assert.match(s,/storeShareTarget/);assert.match(s,/share-target:pending/);assert.match(s,/request\.method === 'POST'/);assert.match(s,/#\/share-target/)})
test('server share target requires authorization and creates draft only',()=>{const s=fs.readFileSync(new URL('../functions/share-target.js',import.meta.url),'utf8');assert.match(s,/content:write/);assert.match(s,/status:'draft'/);assert.match(s,/workflowState:'draft'/);assert.doesNotMatch(s,/status:'published'/)})
