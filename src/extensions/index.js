// Trusted build-time extensions are registered here. Uploaded arbitrary code is
// deliberately not an extension mechanism.
// import { installExampleReadingTimeExtension } from './example-reading-time.js'
// installExampleReadingTimeExtension()
export {}
